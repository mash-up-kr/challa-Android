name: auto assign

on:
  pull_request_target:
    types: [ opened, reopened ]

jobs:
  auto-assign:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: read

    steps:
      - name: set assign
        uses: actions/github-script@v9
        with:
          script: |
            const author = context.payload.pull_request.user.login;
            const prNumber = context.payload.pull_request.number;

            core.info(`actor: ${context.actor}`);
            core.info(`author: ${author}`);
            core.info(`repo: ${context.repo.owner}/${context.repo.repo}`);
            core.info(`prNumber: ${prNumber}`);

            try {
              await github.rest.issues.addAssignees({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: prNumber,
                assignees: [author],
              });

              core.info(`Assigned ${author}`);
            } catch (error) {
              core.warning(`status: ${error.status}`);
              core.warning(`message: ${error.message}`);
              core.warning(`documentation_url: ${error.response?.data?.documentation_url}`);
              core.warning(JSON.stringify(error.response?.data, null, 2));
              throw error;
            }
