name: CI

on:
  pull_request:
    branches: [ develop ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Create local.properties
        env:
          DEBUG_BASE_URL: ${{ secrets.DEBUG_BASE_URL }}
        # assembleDebug는 컴파일만 수행하므로 URL 값은 사용되지 않음. 키 존재 여부만 필요.
        # Secret 미설정 시(fork PR 등) placeholder로 폴백
        run: |
          echo "debug.base.url=${DEBUG_BASE_URL:-https://placeholder.example.com/}" >> local.properties
          echo "release.base.url=${DEBUG_BASE_URL:-https://placeholder.example.com/}" >> local.properties

      - name: ktlint
        run: ./gradlew ktlintCheck

      - name: Build (debug)
        run: ./gradlew assembleDebug
