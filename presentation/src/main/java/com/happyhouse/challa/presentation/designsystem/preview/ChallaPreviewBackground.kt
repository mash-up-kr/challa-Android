package com.happyhouse.challa.presentation.designsystem.preview

/**
 * `@Preview(backgroundColor = ...)`에 쓰는 앱 기본 배경색.
 *
 * 어노테이션 인자는 컴파일 타임 상수만 받을 수 있어
 * `ChallaTheme.colors.backgroundSurface`를 쓸 수 없으므로 같은 값을 상수로 둔다.
 */
const val CHALLA_PREVIEW_BACKGROUND = 0xFF111111L
